.idea*
*.DS_Store
/build/
/out/
/build*/
modules/libtorch*
modules/onnxruntime*
modules/tensorflow*
modules/LiteRt*
modules/executorch*
# Everything under modules/ is fetched (backends, tanh-lib); a checkout may also point the
# directory elsewhere with a symlink, which must never be committed.
/modules
cmake-build*
.vscode
logs/
results/
venv/
web/wasm/
web/dist/
**/node_modules/**
extras/models/stateful-rnn/stateful-lstm/
extras/models/hybrid-nn/GuitarLSTM/
extras/models/cnn/steerable-nafx/
extras/models/model-pool/example-models/
extras/models/third-party/ircam-acids/RAVE/
.cache/

# Auto-generated TypeDoc artifacts for the Anira Web docs (consumed by sphinx-js).
docs/typedoc/node_modules/
docs/typedoc/package-lock.json
# The CI overhaul plan is a local working document (kept out of the repo by decision, 2026-08-31)
docs/ci-overhaul.md

# Clang source-based coverage artifacts. A test binary built with
# -fprofile-instr-generate writes default.profraw into whatever directory it is
# run from, so a run from the repo root drops one here.
*.profraw
*.profdata
