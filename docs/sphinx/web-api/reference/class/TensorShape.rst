Class TensorShape
=================

.. note::
   Construct via the :doc:`AniraWeb` factory in most cases:
   ``aniraWeb.TensorShape(...)`` rather than ``new TensorShape(...)``. The
   factory threads the WASM instance through for you.

.. js:autoclass:: TensorShape.TensorShape
   :short-name:
   :members:
