Class PrePostProcessor
======================

.. note::
   Construct via the :doc:`AniraWeb` factory in most cases:
   ``aniraWeb.PrePostProcessor(...)`` rather than ``new PrePostProcessor(...)``. The
   factory threads the WASM instance through for you.

.. js:autoclass:: PrePostProcessor.PrePostProcessor
   :short-name:
   :members:
