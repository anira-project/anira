Class ProcessingSpec
====================

.. note::
   Construct via the :doc:`AniraWeb` factory in most cases:
   ``aniraWeb.ProcessingSpec(...)`` rather than ``new ProcessingSpec(...)``. The
   factory threads the WASM instance through for you.

.. js:autoclass:: ProcessingSpec.ProcessingSpec
   :short-name:
   :members:
