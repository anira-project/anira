# Generated by anira's cmake/install.cmake at configure time — do not edit.
#
# The anira::<engine> targets of this installed anira, defined with the same
# function and the same usage requirements as in anira's build tree
# (aniraBackendHelpers.cmake, shipped next to this file). Included by
# aniraConfig.cmake before aniraTargets.cmake, so that the $<LINK_ONLY:anira::<engine>>
# link dependencies of a static anira resolve. Everything is relative to
# PACKAGE_PREFIX_DIR (set by aniraConfig.cmake), so the installed tree is relocatable.
#
# anira links these targets PRIVATE: target_link_libraries(x anira::anira) gives x
# anira's headers and USE_* definitions and no engine header. A consumer whose own
# code calls an engine links anira::<engine> in addition and gets the very file
# anira uses — one copy of the engine per process.
#
# This anira: @ANIRA_BACKEND_LINKAGE@ backends (BUILD_SHARED_LIBS=@BUILD_SHARED_LIBS@).

set(_anira_libdir "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_LIBDIR@")
set(_anira_incdir "${PACKAGE_PREFIX_DIR}/@CMAKE_INSTALL_INCLUDEDIR@/anira-backends")

@ANIRA_INSTALLED_BACKEND_TARGETS@
unset(_anira_libdir)
unset(_anira_incdir)
